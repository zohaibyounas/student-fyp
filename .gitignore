# Secrets & local config — never push
.env
.env.*
!.env.example

# Local run scripts (may contain API keys / DB URLs)
*.bat
run_with_db.bat

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
venv/
.venv/
env/

# IDE / OS
.idea/
.vscode/
*.swp
.DS_Store
Thumbs.db

# Dataset CSV may be large — keep local, share separately
data/*.csv
!data/README.txt
